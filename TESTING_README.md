# 测试文档

## 概述

本项目包含三个测试套件，用于验证性能优化的有效性和功能的完整性。

## 测试文件

### 1. test_functional.py - 功能测试
测试所有工具模式、选中、悬停、拖动功能和导入导出功能。

**覆盖的需求：**
- Requirement 4.1: 线条选中功能
- Requirement 4.2: 线条悬停功能
- Requirement 4.3: 控制点拖动功能
- Requirement 4.4: 预览线条绘制
- Requirement 4.5: 导入导出功能

**测试内容：**
- 选择工具模式
- 连线工具模式
- 路径工具模式
- 自由线条工具模式
- 线条选中、悬停、删除
- 控制点拖动
- 导入导出往返
- 撤销功能

### 2. test_performance.py - 性能测试
测试内存拷贝量、响应时间和更新频率。

**覆盖的需求：**
- Requirement 5.1: 10条线的内存拷贝量
- Requirement 5.2: 100条线的内存拷贝量
- Requirement 5.3: 鼠标移动响应时间
- Requirement 5.4: 更新频率节流

**测试内容：**
- 10条线的内存拷贝量测试
- 100条线的内存拷贝量测试
- 内存拷贝量缩放特性（验证O(1)）
- 鼠标移动响应时间测试
- 更新频率节流测试

### 3. test_regression.py - 回归测试
测试所有现有功能未被破坏，测试边界情况。

**覆盖的需求：**
- Requirement 4.1-4.5: 所有功能完整性

**测试内容：**
- 边界情况：空图像、没有线条、没有孔
- 各种线条类型：单条线、大量线条、带中间点的线条、自由线条
- 线条属性：不同颜色、不同线宽
- 线条状态：选中状态、悬停状态
- 功能操作：导出导入往返、撤销序列、清除所有线条

### 4. run_all_tests.py - 测试运行器
运行所有测试套件并生成总结报告。

## 运行测试

### 运行所有测试（推荐）
```bash
python run_all_tests.py
```

这将依次运行功能测试、性能测试和回归测试，并生成总结报告。

### 运行单个测试套件
```bash
# 功能测试
python test_functional.py

# 性能测试
python test_performance.py

# 回归测试
python test_regression.py
```

## 测试结果

所有测试结果记录在 `TEST_RESULTS.md` 文件中。

当前测试状态：
- 功能测试：✓ 10/10 通过
- 性能测试：✓ 5/5 通过
- 回归测试：✓ 14/14 通过
- **总计：✓ 29/29 通过 (100%)**

## 测试环境要求

- Python 3.x
- PyQt5
- OpenCV (cv2)
- NumPy

## 测试原理

### 功能测试
使用单元测试方法，创建测试环境并验证各个功能模块的正确性。

### 性能测试
使用 `tracemalloc` 模块追踪内存分配，使用 `time` 模块测量响应时间。

### 回归测试
测试各种边界情况和正常使用场景，确保优化后的代码不会破坏现有功能。

## 添加新测试

如果需要添加新的测试，请遵循以下步骤：

1. 在相应的测试文件中添加新的测试方法
2. 方法名以 `test_` 开头
3. 在方法中使用 `assert` 语句验证结果
4. 在 `run_all_tests` 方法中添加新测试到测试列表
5. 运行测试验证

示例：
```python
def test_new_feature(self):
    """测试新功能"""
    print("测试: 新功能")
    
    # 设置测试环境
    window = BreadboardConnector()
    window.image = self.create_test_image()
    
    # 执行测试
    result = window.some_new_feature()
    
    # 验证结果
    assert result == expected_value, "新功能测试失败"
    
    print("✓ 新功能测试通过")
    return True
```

## 持续集成

这些测试可以集成到CI/CD流程中，确保每次代码变更都不会破坏现有功能或降低性能。

## 故障排除

### 测试失败
如果测试失败，请检查：
1. 是否安装了所有依赖
2. 代码是否有语法错误
3. 是否修改了核心功能导致测试失败

### 性能测试不稳定
性能测试可能受系统负载影响，如果性能测试偶尔失败：
1. 关闭其他占用资源的程序
2. 多次运行测试取平均值
3. 调整测试阈值（如果合理）

## 联系方式

如有问题或建议，请提交Issue或Pull Request。
